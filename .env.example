# Переменные окружения для локальной разработки
# Скопируйте этот файл в .env и заполните значения

# Node Environment
NODE_ENV=development

# Database Connection (PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
# Для теста платежей (Idram callback): запустите ngrok http 3000 и подставьте сюда URL из ngrok, например:
# NEXTAUTH_URL=https://xxxx.ngrok-free.app
NEXTAUTH_SECRET=your-secret-key-minimum-32-characters-long

# Vercel Blob Storage (обязательно для локальной разработки и production)
# Получить токен: Vercel Dashboard → Storage → Blob → Tokens → Create Token (read-write)
# Оба окружения (локальное и production) используют Blob Store
BLOB_READ_WRITE_TOKEN=your-blob-read-write-token

# Ameria Bank vPOS Configuration
# Test environment credentials (provided by bank)
AMERIA_BANK_ENVIRONMENT=test
AMERIA_BANK_CLIENT_ID=f707bd34-5263-4a35-a5d5-50f67967ba57
AMERIA_BANK_USERNAME=3d19541048
AMERIA_BANK_PASSWORD=lazY2k

# Production credentials (will be provided later)
# AMERIA_BANK_ENVIRONMENT=production
# AMERIA_BANK_CLIENT_ID=your-production-client-id
# AMERIA_BANK_USERNAME=your-production-username
# AMERIA_BANK_PASSWORD=your-production-password

# IDram Payment Configuration
# Register RESULT_URL, SUCCESS_URL, FAIL_URL with Idram (they must be publicly accessible)
IDRAM_TEST_MODE=true
IDRAM_TEST_REC_ACCOUNT=your-test-idram-id
IDRAM_TEST_SECRET_KEY=your-test-secret-key
# Production
# IDRAM_TEST_MODE=false
# IDRAM_REC_ACCOUNT=your-production-idram-id
# IDRAM_SECRET_KEY=your-production-secret-key
# IDRAM_EMAIL=optional-email-for-notifications

# E-HDM (электронные счета, Tax Service AM)
# Сертификат и ключ положить в папку Private/ (она в .gitignore)
EHDM_API_URL=https://ecrm.taxservice.am/taxsystem-rs-vcr/api/v1.0
EHDM_CRN=your-crn
EHDM_TIN=your-tin
EHDM_CERT_PATH=./Private/your-tin.crt
EHDM_KEY_PATH=./Private/your-tin.key
EHDM_KEY_PASSPHRASE=your-passphrase
EHDM_INITIAL_SEQ=1
# Код АТГ по умолчанию для позиций чека (из перечня ПЕК; один на весь магазин)
EHDM_DEFAULT_ADG_CODE=2201

# Примечание: PORT, NEXT_PUBLIC_API_URL, NEXT_PUBLIC_SITE_URL не нужны для Vercel
# Vercel автоматически определяет эти значения
